name: Build UF2
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install deps
        run: sudo apt update && sudo apt install -y cmake gcc-arm-none-eabi libnewlib-arm-none-eabi build-essential git libstdc++-arm-none-eabi-newlib
      - name: Clone SDKs
        run: |
          rm -rf pico-sdk PicoDVI build
          git clone --branch 2.2.0 --depth 1 https://github.com/raspberrypi/pico-sdk.git pico-sdk
          git -C pico-sdk submodule update --init
          git clone --depth 1 https://github.com/Wren6991/PicoDVI.git PicoDVI
          git -C PicoDVI submodule update --init
      - name: Build
        env:
          PICO_SDK_PATH: ${{ github.workspace }}/pico-sdk
        run: |
          cmake -B build -S. -DPICO_BOARD=waveshare_rp2350_pizero -DPICO_PLATFORM=rp2350-arm-s -DPICODVI_PATH=${{ github.workspace }}/PicoDVI -DCMAKE_BUILD_TYPE=Release
          cmake --build build -j4 --verbose
      - name: Upload UF2
        uses: actions/upload-artifact@v4
        with:
          name: famicom-hdmi-uf2
          path: build/*.uf2